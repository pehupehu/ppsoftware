<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}{{ 'login.title'|trans }}{% endblock %}</title>

        <link rel="stylesheet" href="{{ asset('build/bootstrap-4.1.3-dist/css/bootstrap.min.css') }}">
        <link rel="stylesheet" href="{{ asset('build/open-iconic/font/css/open-iconic-bootstrap.css') }}">
        <link rel="stylesheet" href="{{ asset('build/jquery-ui-1.12.1/jquery-ui.min.css') }}">
        <link rel="stylesheet" href="{{ asset('build/jquery-ui-1.12.1/jquery-ui.theme.min.css') }}">
        <link rel="stylesheet" href="{{ asset('build/flags/flags.min.css') }}">
        <link rel="stylesheet" href="{{ asset('build/css/login.css') }}">
        <script src="{{ asset('build/jquery-3.3.1.min.js') }}"></script>
        <script src="{{ asset('build/jquery-ui-1.12.1/jquery-ui.min.js') }}"></script>
        <script src="{{ asset('build/popper.min.js') }}"></script>
        <script src="{{ asset('build/bootstrap-4.1.3-dist/js/bootstrap.min.js') }}"></script>
    </head>
    <body>
        <form class="form-signin" action="{{ path('login') }}" method="post">
            <h1 class="h3 mb-3 font-weight-normal">{{ 'login.title'|trans }}</h1>

            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

            <label for="username" class="sr-only">{{ 'login.field.username'|trans }}</label>
            <input type="text" id="username" name="_username" value="{{ last_username }}" class="form-control" placeholder="{{ 'login.field.username'|trans }}" autocomplete="off" required autofocus>
            <label for="password" class="sr-only">{{ 'login.field.password'|trans }}</label>
            <input type="password" id="password" name="_password" class="form-control" placeholder="{{ 'login.field.password'|trans }}" required>
            <div class="checkbox mb-3">
                <label>
                    <input type="checkbox" value="remember-me"> {{ 'login.field.remember_me'|trans }}
                </label>
            </div>

            {#
                If you want to control the URL the user
                is redirected to on success (more details below)
                <input type="hidden" name="_target_path" value="/account" />
            #}

            <button class="btn btn-lg btn-primary btn-block" type="submit">{{ 'login.action.login'|trans }}</button>
            <p class="mt-5 mb-3 text-muted">&copy; 2017-2018</p>
        </form>
    </body>
</html>
